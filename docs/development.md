# Development Guide

This is the **index** for internal development knowledge. It is `@`-imported into `CLAUDE.md`,
so the routing table below is always in context — use it to find the right reference before
starting work. Keep this file short; put detail in `docs/dev/*.md`.

Most code conventions are **not** listed here: they live in `.claude/rules/*.md` with `paths:`
frontmatter and load by themselves when you open a matching file (Python, tests, e2e, the web
layer, docs, hooks). The table below carries only what no file pattern can trigger.

## Setup

```bash
uv sync
```

Run the CLI from a clone:

```bash
uv run ojhunt tourist@codeforces
uv run ojhunt --list
uv run ojhunt --json tourist@codeforces
```

## Where to look

Read the matching reference **before** implementing or debugging in that area.

| Working on… | Read |
|-------------|------|
| Crawler reference (metadata schema, login types, return format, templates, HTML parsing, SSL, license, archived rules) | [`docs/dev/crawlers.md`](dev/crawlers.md) |
| **Implementing / debugging** a crawler (procedure, decision tree, checklist) | **ojhunt-crawlers** skill |
| Dev server: start/stop, ports in worktrees, env vars | [`docs/dev/web.md`](dev/web.md) |
| Deployment / ops: system fonts, `legacy.db`, PDF preview | [`docs/dev/deployment.md`](dev/deployment.md) |
| Committing, commit conventions, when to write an ADR | **ojhunt-commit** skill |
| Where a new piece of knowledge belongs (rule vs docs vs skill vs hook vs ADR) | **ojhunt-update-env** skill |

A crawler is the one code area still routed by this table rather than by a rule: a new crawler
starts as a file that does not exist yet, so no `paths:` pattern can fire on it.

User-facing usage docs (not development conventions): [`docs/cli.md`](cli.md),
[`docs/web.md`](web.md), [`docs/library.md`](library.md) — the last one is generated by
`./doit.sh gen-docs`, so regenerate it rather than editing it.

Known follow-ups deliberately left out of the change that found them:
[`docs/BACKLOG.md`](BACKLOG.md).

## Architecture Decisions

Significant architectural decisions and their rationale are recorded in [`docs/adr/`](./adr/).

- [ADR 0001](./adr/0001-enrich-query-response-schema.md) — Enrich query response schema and add `/api/merge`
- [ADR 0002](./adr/0002-agent-support-via-llmstxt.md) — Agent support via `/llms.txt`
- [ADR 0003](./adr/0003-rename-is-virtual-judge-to-is-aggregator.md) — Rename `isVirtualJudge` to `isAggregator`
- [ADR 0004](./adr/0004-history-pdf-backup.md) — History tracking via PDF backup/restore (no database)
- [ADR 0005](./adr/0005-localstorage-watch-config-only.md) — localStorage sync via $watch (config-only persistence)
- [ADR 0006](./adr/0006-legacy-lookup-abp-username-only.md) — legacy lookup restricted to ABP username only
- [ADR 0007](./adr/0007-label-cache-is-load-bearing.md) — label cache in `nit`/`uva` is load-bearing; those crawlers require the full package
- [ADR 0008](./adr/0008-unique-solved-dedup-design.md) — unique solved dedup is problem-level; listless crawlers add raw count directly
- [ADR 0009](./adr/0009-parallel-worktree-dev-server.md) — parallel-worktree dev server: dynamic port per worktree + orphan reaping (no git hook)
- [ADR 0010](./adr/0010-relaxed-csp-for-inline-alpine.md) — relaxed CSP (`script-src` allows `'unsafe-inline'`/`'unsafe-eval'`) is load-bearing for inline Alpine.js; don't tighten without migrating to `@alpinejs/csp`
- [ADR 0011](./adr/0011-poj-status-log-fallback.md) — POJ falls back to walking the public `/status` log when `/userstatus` is 403-blocked; the primary `/userstatus` path is load-bearing, don't delete it
- [ADR 0012](./adr/0012-identify-outbound-requests.md) — all crawler requests go through `create_session()`, which adds an OJHunt `User-Agent` + always-on `X-OJHunt` header naming the repo (`OJHUNT_INSTANCE_URL` adds the operator's own URL); identity lives at the session layer, never in crawler files
- [ADR 0013](./adr/0013-lazy-crawler-registry.md) — the registry is the module attribute `ojhunt.crawlers.crawlers`, discovered on first access; the `TYPE_CHECKING` declaration is load-bearing for ruff F822
- [ADR 0014](./adr/0014-generated-crawler-help.md) — crawler `help()` text is generated from `__crawler_meta__` and attached to `CrawlerInfo.__doc__`; crawler module docstrings stay reserved for the license header
- [ADR 0015](./adr/0015-submissions-floor-is-solved.md) — every crawler reports at least its `solved` count as `submissions`, so the figure is a lower bound; the rule lives in the crawler files, not in `CrawlerResult`
- [ADR 0016](./adr/0016-adopt-ruff-default-rule-set.md) — ruff's default rule set stands unpinned, which reversed the typing convention to PEP 585/604; the five exceptions (`BLE001`, `fastapi.File`, and three `noqa`s) are listed there
